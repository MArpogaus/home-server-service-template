# Quadlet applies this after the .container file, so a key set here wins over
# the same key in a unit. Per-container settings must use a different key.
[Container]
AutoUpdate=registry

[Service]
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=default.target
